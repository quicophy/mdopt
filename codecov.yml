coverage:
  status:
    project:
      default:
        target: auto
        threshold: 90%
        base: auto
    patch:
      default:
        target: 90%
        threshold: 0%
        base: auto

ignore:
  - "examples/"
